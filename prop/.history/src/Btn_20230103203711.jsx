import React, { useState } from 'react'

function Btn(props) {
  const {setFlag} = props
  const handleClickBtn= () => {
  
  }
  return (
    <button onClick={handleClickBtn}>button</button>
  )
}

export default Btn